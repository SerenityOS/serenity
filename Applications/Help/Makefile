OBJS = \
    ManualModel.o \
    ManualSectionNode.o \
    ManualPageNode.o \
    History.o \
    main.o

PROGRAM = Help

LDFLAGS = -lgui -lhtml -lmarkdown -ldraw -lipc -lprotocol -lthread -lpthread -lcore -lc

include ../../Makefile.common
